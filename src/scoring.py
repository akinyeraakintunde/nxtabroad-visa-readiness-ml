# Example: loading sample profiles from CSV (optional demo)
# import pandas as pd
# df = pd.read_csv("data/raw/visa_profiles_raw.csv")
# for _, row in df.iterrows():
#     profile_dict = row.to_dict()
#     result = score_profile(profile_dict)
#     print(profile_dict["profile_id"], result["overall_score"], result["readiness_band"])
