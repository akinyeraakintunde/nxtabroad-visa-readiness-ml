# API Overview

The API is powered by FastAPI (`api/demo_fastapi.py`).

- Endpoint: `POST /predict`
- Request: JSON with `profiles` list
- Response: `results` with scores and explanations

See: `README_API.md`, `data/sample_request.json`, `data/sample_response.json`.